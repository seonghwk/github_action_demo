name: Create File
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create File
        run: |
          date >> test.txt

      - name: Show file content
        run: cat test.txt

      - name: Show current info
        run: hostname -I
        run: ls -al
